# VOLTA Corporate — Material Debt

| ID | Priority | Area | Evidence / impact | Resolution | Status |
|---|---|---|---|---|---|
| CORP-DEBT-001 | P2 | Documentation | Repository README was generic create-next-app boilerplate and provided no VOLTA context. | Replaced during Product OS adoption. | RESOLVED |
| CORP-DEBT-002 | P1 | Content operations | Product facts and lifecycle labels remain hard-coded in `src/app/page.tsx` while Store/Booking/Portfolio evolve independently. `verify:corporate` now protects known URLs/labels, but it does not automatically consume the central registry. | Review registry/Product OS before material corporate changes; later consider a small typed/build-time product manifest when the registry contract is stable. | OPEN |
| CORP-DEBT-003 | P1 | Deployment | Merge `6e05ebef` to `main` did not trigger a Vercel deployment. The project previously reported `live: false`; current production was already being published through a manual API/bootstrap workaround. A new manual publish attempt on 2026-08-25 failed with Vercel `402 payment_required` because the Hobby `api-deployments-free-per-day` limit was exhausted (100/100). | Repair GitHub→Vercel integration and prove a normal `main` push creates a production deployment with Git commit metadata. Manual API publishing may bridge releases but does not resolve this debt. | OPEN |
| CORP-DEBT-004 | P3 | Code hygiene | `CORP-INIT-003` removed the verified-unused WhatsApp-era section/content/UI source tree (~1,000 lines). `clsx`, `framer-motion` and `tailwind-merge` still remain in `package.json`/lock despite no current Main 2.0 source references found during the audit. | Remove the residual unused dependencies together with a lockfile regeneration, then let the quality gate prove clean install/lint/build. | PARTIALLY RESOLVED |
| CORP-DEBT-005 | P1 | Conversion / cross-product dependency | Store, Portfolio and Booking all expose authoritative production URLs in VOLTA OS. Corporate `main` now wires all three destinations. | Keep routing aligned with registry/Product OS. | RESOLVED |
| CORP-DEBT-006 | P2 | Repository metadata | GitHub repository description still says `Landing de VOLTA — sistema que automatiza ventas y atención en WhatsApp.`, contradicting the current ecosystem-first role. The connected GitHub tool used in this review does not expose repository-description writes. | Update the GitHub repository description through GitHub settings or another authorized repository-metadata path, then verify it no longer frames VOLTA as WhatsApp-only. | OPEN |

Do not create speculative technical debt for this lightweight site without repository, Product OS or production evidence.
